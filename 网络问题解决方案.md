# 🌐 网络连接问题解决方案

## 🔍 问题诊断

测试结果显示：
```
Error message: [GoogleGenerativeAI Error]: Error fetching from https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent: fetch failed
```

这是一个**网络连接问题**，通常发生在企业网络环境中。

---

## 📋 解决方案

### 方案 1：检查网络连接

```bash
# 测试网络连接
ping google.com

# 测试 Gemini API 端点
curl https://generativelanguage.googleapis.com/
```

### 方案 2：配置代理（如果您在企业网络中）

1. 创建或更新 `.env.local` 文件：

```env
# Google Gemini API Key
GEMINI_API_KEY=AIzaSyBXkqivcwJG1N-ZCJzN_2gfzhvT2wjWMb0

# 代理配置（如果需要）
HTTP_PROXY=http://your-proxy-server:port
HTTPS_PROXY=http://your-proxy-server:port
NO_PROXY=localhost,127.0.0.1
```

2. 更新 Next.js 配置以使用代理：

```javascript
// next.config.js
module.exports = {
  images: {
    domains: ['generativelanguage.googleapis.com'],
  },
  async rewrites() {
    return [];
  },
};
```

### 方案 3：使用手机热点测试

如果您在企业网络中，可以尝试：
1. 连接手机热点
2. 再次运行测试：
```bash
node test-api.js
```

### 方案 4：检查防火墙设置

联系您的 IT 部门，确保以下域名没有被阻止：
- `generativelanguage.googleapis.com`
- `*.googleapis.com`

### 方案 5：使用备用 API 端点

让我更新代码以添加更好的错误处理和备用方案：

---

## 🚀 测试连接

运行以下命令测试修复是否生效：

```bash
cd "d:\测试\ZENSHORTS AI 测试"
node test-api.js
```

如果成功，您会看到：
```
✅ SUCCESS! Gemini API is working correctly.
```

---

## 📝 常见问题

### Q: 为什么会出现这个错误？
A: 这通常是由于企业防火墙阻止了对外 API 调用，或者需要配置代理服务器。

### Q: 如何确认是网络问题？
A: 尝试访问 https://generativelanguage.googleapis.com/ 在浏览器中，如果无法访问就是网络问题。

### Q: 有没有不需要网络的解决方案？
A: 目前没有，因为 AI 模型运行在云端，需要网络连接。

---

如果问题仍然存在，请告诉我您的网络环境（家庭/企业），我可以提供更针对性的解决方案。
